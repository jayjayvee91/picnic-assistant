// Entry point — intentionally minimal in Step 1.
// Real wiring (Picnic adapter, memory, agent, Telegram, scheduler) lands in later steps.

function main(): void {
  // Plain Dutch boot message so a `npm run dev` smoke test confirms the toolchain works.
  console.log('picnic-assistant: scaffolding klaar. Geen actieve bot in deze stap.');
}

main();
